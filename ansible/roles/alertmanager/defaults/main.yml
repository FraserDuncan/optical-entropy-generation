---
# Alertmanager role defaults

alertmanager_version: "0.26.0"
alertmanager_user: alertmanager
alertmanager_group: alertmanager

alertmanager_install_dir: /opt/alertmanager
alertmanager_config_dir: /etc/alertmanager
alertmanager_data_dir: /var/lib/alertmanager

alertmanager_listen_address: "0.0.0.0:9093"

# Notification settings (override in inventory)
alertmanager_smtp_smarthost: "smtp.example.com:587"
alertmanager_smtp_from: "alertmanager@example.com"
alertmanager_slack_webhook_url: ""
