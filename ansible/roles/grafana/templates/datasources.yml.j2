# Datasource provisioning - managed by Ansible
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: {{ prometheus_url }}
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "5s"
      httpMethod: POST
